<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.badminton_team.dao.PostDao">

    <resultMap id="BaseResultMap" type="com.example.badminton_team.entity.Post">
        <id column="pid" property="pid" />
        <result column="user_id" property="userId" />
        <result column="authorName" property="authorName" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="view_count" property="viewCount" />
        <result column="like_count" property="likeCount" />
        <result column="comment_count" property="commentCount" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        p.pid, p.user_id, u.nickname AS authorName, p.title, p.content,
        p.view_count, p.like_count, p.comment_count, p.status,
        p.create_time, p.update_time
    </sql>

    <!-- 查询帖子详情 -->
    <select id="findByPid" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM post p
        LEFT JOIN user u ON p.user_id = u.uid
        WHERE p.pid = #{pid}
    </select>

    <!-- 查询列表（不分页） -->
    <select id="findList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM post p
        LEFT JOIN user u ON p.user_id = u.uid
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <if test="status == null">
                AND p.status != 2
            </if>
            <if test="keyword != null and keyword != ''">
                AND (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <!-- 分页查询 -->
    <select id="findPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM post p
        LEFT JOIN user u ON p.user_id = u.uid
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY p.create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计总数 -->
    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM post p
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <!-- 查询用户发布的帖子 -->
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM post p
        LEFT JOIN user u ON p.user_id = u.uid
        WHERE p.user_id = #{userId} AND p.status != 2
        ORDER BY p.create_time DESC
    </select>

    <!-- 插入帖子 -->
    <insert id="insert" parameterType="com.example.badminton_team.entity.Post" useGeneratedKeys="true" keyProperty="pid">
        INSERT INTO post (user_id, title, content, view_count, like_count,
                          comment_count, status, create_time, update_time)
        VALUES (#{userId}, #{title}, #{content}, 0, 0, 0,
                #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.badminton_team.entity.Post">
        UPDATE post
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            update_time = NOW()
        </set>
        WHERE pid = #{pid}
    </update>

    <update id="updateStatus">
        UPDATE post SET status = #{status}, update_time = NOW() WHERE pid = #{pid}
    </update>

    <update id="increaseViewCount">
        UPDATE post SET view_count = view_count + 1 WHERE pid = #{pid}
    </update>

    <update id="increaseLikeCount">
        UPDATE post SET like_count = like_count + 1 WHERE pid = #{pid}
    </update>

    <update id="decreaseLikeCount">
        UPDATE post SET like_count = like_count - 1 WHERE pid = #{pid} AND like_count > 0
    </update>

    <update id="increaseCommentCount">
        UPDATE post SET comment_count = comment_count + 1 WHERE pid = #{pid}
    </update>

    <update id="decreaseCommentCount">
        UPDATE post SET comment_count = comment_count - 1 WHERE pid = #{pid} AND comment_count > 0
    </update>

    <!-- 点赞表操作 -->
    <select id="hasLiked" resultType="boolean">
        SELECT COUNT(*) > 0 FROM post_like WHERE post_id = #{postId} AND user_id = #{userId}
    </select>

    <insert id="addLike">
        INSERT INTO post_like (post_id, user_id) VALUES (#{postId}, #{userId})
    </insert>

    <delete id="removeLike">
        DELETE FROM post_like WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM post
    </select>

    <select id="getLikeCount" resultType="int">
        SELECT like_count FROM post WHERE pid = #{pid}
    </select>
</mapper>